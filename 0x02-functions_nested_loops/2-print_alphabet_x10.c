#include "main.h"

/**
 * main - prints 10x the alphabet, in loweercase
 *
 * Return: Always 0
 */
int main(void)
{
	char c, i;

	for (i = 0; i <= 9; i++)
	{
		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(C);
		}
		_putchar('\n');
	}
}
