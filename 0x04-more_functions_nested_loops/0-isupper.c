#include "main.h"

/**
 * _isupper - checks for uppercase
 * @c: charater to check.
 * Return:1 if cis uppercase, otherwise return 0.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	esle
		return (0);
}
